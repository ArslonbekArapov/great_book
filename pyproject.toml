[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "arslonbek-tools"
version = "0.1.0"
description = "A simple Python utility library by Arslonbek"
readme = "README.md"
authors = [{name = "Arslonbek", email = "arslonbekdev@gmail.com"}]
license = {text = "MIT"}
requires-python = ">=3.7"
